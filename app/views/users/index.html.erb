<h1>用户列表</h1>

<table>
  <thead>
  <tr>
    <th>名称</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= link_to '显示', user %></td>
        <td><%= link_to '编辑', edit_user_path(user) %></td>
        <td><%= link_to '删除', user, method: :delete, data: { confirm: '确定删除?' } %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to '首页', root_path %> | <%= link_to '新建账户', new_user_path %>
