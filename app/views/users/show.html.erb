<p id="notice"><%= notice %></p>

<p>
  <strong>名称:</strong>
  <%= image_tag(@user.image_url(:thumb)) %>
  <%= image_tag(@user.avatar_url(:big)) if @user.avatar? %>
  <!--这里的:large就是指定图片的版本为large 64x64大小-->
  <%= @user.name %>
</p>

<%= link_to '编辑', edit_user_path(@user) %>